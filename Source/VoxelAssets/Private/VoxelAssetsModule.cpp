// VoxelAssets has no custom module-level state - just UCLASS registration,
// which the engine's reflection system handles automatically.

#include "Modules/ModuleManager.h"

IMPLEMENT_MODULE(FDefaultModuleImpl, VoxelAssets)
